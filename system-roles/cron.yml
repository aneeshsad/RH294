---
- name: Create Cron
  hosts: all
  become: true
  tasks:
    - name: Create Cron
      ansible.builtin.cron:
        name: EX294 in progress
        minute: "*/2"
        user: student
        job: "logger 'EX294 in progress'"
