[defaults]
inventory = ./inventory
remote_user = devops
ask_pass = false
roles_path= /home/student/system-roles/roles
collections_path=./collections:/usr/share/ansible/collections
[privilege_escalation]
become = true
become_method = ansible.builtin.sudo
become_user = root
become_ask_pass = false
